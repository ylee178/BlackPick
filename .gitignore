node_modules/
.next/
.vercel/
.env
.env.local
data/
/tmp/
*.log
.DS_Store

# TypeScript incremental build cache — binary, per-machine, never useful in git
*.tsbuildinfo

# Local email template preview fixtures — not for shipping, not localized
public/email/previews/

# File-based dialogue review transcripts live at
# BlackPick/reviews/BlackPick/<date>_<topic>_dialog/round_<N>_{prompt,review}.md
# per the convention in Docs/review-tier-rubric.md § File-based dialogue
# pattern. The inner BlackPick/ subfolder is a project-isolation visual
# guard per the generic usage doc at ~/Desktop/Wiki_Sean/Tech/
# second_opinion_reviewer_usage.md. Raw transcripts stay local; session-
# log summaries continue to live at the external Wiki_Sean path per the
# feedback_wiki_log_location memory entry.
reviews/

# Session logs live OUTSIDE the project repo at
# `/Users/uxersean/Desktop/Wiki_Sean/BlackPick/`. Block the in-repo
# path so a stray write never lands in a commit. Primary rule is
# "write to the external path" — see CLAUDE.md §Session start.
Wiki_Sean/

*storybook.log
Fighter_Images/
storybook-static

# Playwright
e2e/test-results/
e2e/html-report/
e2e/.auth/
.vercel

# remove.bg batch test artifacts
remove_background/input/
remove_background/output/
remove_background/logs/
