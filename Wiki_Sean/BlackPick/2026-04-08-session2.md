# Session 2 — 2026-04-08

## Branch: `feature/fighter-image-management`

## Workflow
- agent-skills 패턴 적용: GPT 5.4 spec → 비판적 분석 → plan → build → GPT 5.4 review
- GPT 5.4 API 호출로 코드 리뷰 및 검증 수행

## Completed Tasks

### BLOCKER Security (#2-4)
- fighter-avatar API 4개 엔드포인트에 `getUser()` 인증 추가
- delete/remap에서 UUID 정규식 검증 (path traversal 차단)
- auth callback `next` 파라미터 open redirect 차단 (`/` 시작 + `//` 차단)

### HIGH Security (#5-7)
- `src/lib/rate-limit.ts` 생성: in-memory sliding-window rate limiter + `rateLimitResponse()` 헬퍼 (429 + Retry-After 헤더)
- fighter-avatar/generate: 5 req/hour
- comments, fighter-comments: 20 req/min
- predictions: 30 req/min
- generate에서 Gemini API키 .env 파싱 → `process.env` 전환 (#6)
- generate에서 fighter_id UUID 검증 추가 (GPT 리뷰 반영)
- findSourcePhoto manifest path traversal 차단 (GPT 리뷰 반영)

### Legal Pages (#8-9)
- `/terms` 이용약관 페이지 (ko/en 자동 전환, 10조)
- `/privacy` 개인정보처리방침 (ko/en, 수집항목/위탁/쿠키/권리 등 10항)
- footer 링크 추가 + 4개 언어 i18n

### Infra/Ops (#10-15)
- #10: error.tsx (retro UI, prod 스택 미노출), not-found.tsx (404), global-error.tsx
- #11: robots.ts (prod/preview 분리), sitemap.ts (6 routes), layout.tsx OG/Twitter meta, metadataBase
- #12: CORS middleware (API routes only, env 기반 origin whitelist)
- #13: env validation (instrumentation.ts에서 필수 env 체크), .env.example
- #14: Sentry config files 생성 (.bak — 패키지 설치 전 보류)
- #15: docs/backup-rollback.md

### Performance HIGH (#16-22)
- #16: home/events/fighters에서 force-dynamic → ISR (revalidate 60/300초)
- #17: fighters, admin에 .limit(500) 추가
- #18: admin fighters select → 필요 컬럼만 (admin events는 타입 제약으로 select * 유지)
- #19: events/[id]/loading.tsx 스켈레톤 추가
- #20: FighterAvatar <img> → Next.js <Image> (size prop, unoptimized for pixel art)
- #21: fs.readdirSync → src/lib/pixel-files.ts (server-only, 60s TTL 캐시)
- #22: ranking API s-maxage=300, events/stats API s-maxage=60

### Performance MEDIUM (#23-26)
- #24: ScoreTrendChart, FighterImageManager dynamic import
- #25: results, dashboard, terminal loading.tsx 추가

### Bonus
- DB 타입에 fighter_comments, fighter_comment_likes 테이블 추가 (빌드 에러 수정)
- getSiteUrl() 연산자 우선순위 버그 수정 (GPT 리뷰 발견)

## GPT 5.4 Reviews
1. 보안 코드 리뷰: UUID 검증 누락, manifest path traversal, execSync 위험 등 지적 → 반영
2. Rate limiter 리뷰: in-memory 한계, Retry-After 헤더 누락, MAX_KEYS eviction 문제 → Retry-After 반영
3. Infra 검증: getSiteUrl() 우선순위 버그 발견 → 수정, prod console.error → dev 전용으로 변경
4. 전체 검증: 모든 태스크 PASS

## Remaining
- 기능 갭: Badge UI (9개 뱃지), @mention autocomplete (FighterComments), Perfect Card 뱃지
- #23 axios 제거 (scripts only, 스킵)
- #26 불필요 "use client" 정리
- #27-32 접근성/기타
- #1 API키 로테이션 → .env가 Git에 커밋된 적 없어 불필요로 판단

## Build
- `next build` 성공 확인
